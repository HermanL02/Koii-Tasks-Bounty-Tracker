version: '3.8'
services:
  service1:
    image: hermanyiqunliang/taskmonitor
    env_file:
      - .env.bounty
    restart: always  # 设置服务退出时总是重启

  service2:
    image: hermanyiqunliang/nodesmonitor
    env_file:
      - .env.nodes
    restart: always  # 设置服务退出时总是重启
